# Frontend (Vite)
# For local dev, you can omit these and it will default to http://localhost:5000

# Base URL for REST API (no trailing /api)
VITE_API_BASE_URL=http://localhost:5000

# Socket.IO server URL (defaults to VITE_API_BASE_URL if omitted)
VITE_SOCKET_URL=http://localhost:5000

